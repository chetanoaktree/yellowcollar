import { useDispatch, useSelector } from "react-redux";
const page_process = ({payment}) => { 
  const dispatch = useDispatch();    
  dispatch({
    type: PAGE_PROCESS_SET,
    payload: {payment:true},
  });   
  return user_
}
const page_process_test = () => {  
  return true
}
export {
  page_process_test  
}
